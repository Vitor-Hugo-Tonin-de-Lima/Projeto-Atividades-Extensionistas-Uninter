export const createActivity = async (req, res) => {
    // Apenas professores podem criar
};

export const getActivities = async (req, res) => {
    // Professores: veem atividades que criaram
    // Alunos: veem atividades atribuídas
};